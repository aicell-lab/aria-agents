apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aria-agents.fullname" . }}-test-connection"
  labels:
    {{- include "aria-agents.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Values.testUrl }}']
  restartPolicy: Never
