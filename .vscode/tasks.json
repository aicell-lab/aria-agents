{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start MinIO",
            "type": "shell",
            "command": "docker-compose -f docker/docker-compose.yml up -d --no-recreate minio",
            "problemMatcher": [],
        },
        {
            "label": "Stop",
            "type": "shell",
            "command": "docker-compose -f docker/docker-compose.yml down",
            "problemMatcher": [],
        }
    ]
}