apiVersion: v1
kind: Service
metadata:
  name: {{ include "aria-agents.fullname" . }}
  labels:
    app: {{ include "aria-agents.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "aria-agents.fullname" . }}
